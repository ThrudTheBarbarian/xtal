void main()
	[
	s32 a;
	u8 b;
	s32 c;
	u16 d;
	s8 e;
	s16 f;
	
	a = 1000;
	b = 5;
	d = 2000;
	e = -2;
	f = -4000;
	
	c = a * b;

	if (c != 5000)
		[ print 0; ]
	else
		[ print 1; ]
	
	c = b * a;

	if (c != 5000)
		[ print 0; ]
	else
		[ print 1; ]
	
	c = d * a;

	if (c != 2000000)
		[ print 0; ]
	else
		[ print 1; ]
	
	c = b * d;

	if (c != 10000)
		[ print 0; ]
	else
		[ print 1; ]
	
	c = e * b;

	if (c != -10)
		[ print 0; ]
	else
		[ print 1; ]

	
	c = e * a;

	if (c != -2000)
		[ print c; ]
	else
		[ print 1; ]

	
	c = e * f;

	if (c != 8000)
		[ print 0; ]
	else
		[ print 1; ]

	
	f = -40;
	c = f * f;

	if (c != 1600)
		[ print 0; ]
	else
		[ print 1; ]
	
	f = -4000;
	c = b * f;

	if (c != -20000)
		[ print 0; ]
	else
		[ print 1; ]

	]
