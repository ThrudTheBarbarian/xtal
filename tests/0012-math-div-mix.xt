#import <stdio>

void main()
	[
	s32 a;
	u8 b;
	s32 c;
	u16 d;
	s8 e;
	s16 f;
	u32 g;
	
	a = 1000;
	b = 5;
	d = 500;
	e = 9;
	f = -4000;
	g = 200000;
	
	c = a / b;

	if (c != 200)
		[ print 0; ]
	else
		[ print 1; ]
	
	c = b / a;

	if (c != 0)
		[ print 0; ]
	else
		[ print 1; ]

	a = 4294966296;
	b = 5;
	c = a/b;

	if (c != 4294967096)
		[ print 0; ]
	else
		[ print 1; ]
	
	a = 1000;
	b = 5;
	d = 500;

	c = d / a;
	if (c != 0)
		[ print c; ]
	else
		[ print 1; ]

	c = a / d;
	if (c != 2)
		[ print c; ]
	else
		[ print 1; ]

	c = d / b;
	if (c != 100)
		[ print c; ]
	else
		[ print 1; ]

	c = b / d;
	if (c != 0)
		[ print c; ]
	else
		[ print 1; ]

	c = a / e;
	if (c != 111)
		[ print c; ]
	else
		[ print 1; ]

	c = d / e;
	if (c != 55)
		[ print c; ]
	else
		[ print 1; ]

	c = f / d;
	if (c != -8)
		[ print c; ]
	else
		[ print 1; ]

	c = 20000;
	c = c / f;
	if (c != -5)
		[ print c; ]
	else
		[ print 1; ]


	c = g / d;
	if (c != 400)
		[ print c; ]
	else
		[ print 1; ]

	c = g / a;
	if (c != 200)
		[ print c; ]
	else
		[ print 1; ]
	]
